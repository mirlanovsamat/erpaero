{
  "name": "erp-aero-test",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "========== infra ==========": "========== infra ==========",
    "docker:up": "set -a && . ./.env && set +a && docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:clean-down": "docker-compose down -v",
    "========== start ==========": "========== start ==========",
    "start": "set -a && . ./.env && set +a && node ./src/app.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "env-var": "^7.4.2",
    "express": "~4.16.1",
    "joi": "^17.13.1",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.9.7"
  }
}
